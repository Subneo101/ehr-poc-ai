
  # EHR Integration Flow-v1-ai(Copy)

  This is a code bundle for EHR Integration Flow-v1-ai(Copy). The original project is available at https://www.figma.com/design/OzPIWe2sH7qwSKNbyR2dxo/EHR-Integration-Flow-v1-ai-Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  